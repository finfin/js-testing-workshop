const {sum, subtract} = require('../math')

let result, expected

result = sum(3, 7)
expected = 10

// TODO: 如果預期結果不等於實際結果，就拋出一個錯誤
//       `${result} is not equal to ${expected}`

result = subtract(7, 3)
expected = 4

// TODO: 如果預期結果不等於實際結果，就拋出一個錯誤
//       `${result} is not equal to ${expected}`
